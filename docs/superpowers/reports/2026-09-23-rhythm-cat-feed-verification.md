# 节奏猫咪验证记录

独立场景：`assets/gamescene/RhythmCatFeedGameScene.scene`。主页入口：节奏猫咪。

## 已实现

- 黑白双猫分别绑定手指，跨中线不换猫；手指取消、后台和广告会清理归属。
- 0 分、3 心；接住加 1，漏接扣 1；32.666667 秒片段、失败／成功结算、重开与返回。
- 视频原像素分离的黑白猫、冰淇淋、爱心、碎屑和背景；使用原录屏混合音轨。
- 编辑器预置 96 个食物节点、24 个碎屑节点、两只猫、嘴部判定点、移动边界、HUD、音频与结算面板。猫咪初始位置、边界、判定半径及下落速度可调整。
- 推荐流预览不消耗时间；首次触摸／正式进入启动；退出、后台、全屏广告的暂停原因可叠加。场景独占音乐期间不会恢复大厅 BGM。
- 增加主页入口、场景 UUID、广告归因名称和非空 Content_ID 精确匹配路由。

## 自动验证

以下新增检查通过：

- `test_rhythm_cat_assets.cjs`：透明通道、逐像素原 RGB 一致性、元数据和音频时长。
- `test_rhythm_cat_rules.cjs`：一次性判定、0 时刻、失败与成功、最后一心、同时间双猫命中、30/60 FPS 与延迟帧插值。
- `test_rhythm_cat_input.cjs`：双指、重复起始、越中线、同侧第三指、取消／复用和非法坐标。
- `test_rhythm_cat_timeline.cjs`：重叠暂停、静音、播放锁、进度回跳及异常值。
- `test_rhythm_cat_controller.cjs`：静态预览、重复进入、双猫控制、暂停交错、重开、导航失败恢复、碎屑跟随当前坐标与销毁清理。
- `test_rhythm_cat_scene.cjs`：序列化引用、对象池容量、音轨、谱面范围和主页入口。
- `test_rhythm_cat_integration.cjs`：音乐接管释放、已配置 ID／空 ID／复访路由，以及重复添加卡片不改变场景。

TypeScript 全项目检查和 `git diff --check` 通过。已有企鹅、推荐流、音频、主页、广告归因、口算和已移除玩法检查也通过；更新了既有的场景／卡片白名单，保留其他任务的改动。

## 构建与浏览器

Cocos Creator 3.8.5 的 web-mobile 构建日志报告 `build success`，已生成并实际加载构建产物。Creator 命令行最终退出码为 36，记录此差异，不将其表述为进程退出码 0。

本机浏览器实际检查：主页进入、拖动黑猫、三心失败、再次游玩恢复三心、返回大厅。375×667、375×740、375×812、375×900 的竖屏布局均能显示玩法和按钮；等价于设计尺寸 750×1334、750×1480、750×1624、750×1800。浏览器检查期间未记录控制台警告或错误。

双指并行、广告／后台交错通过独立控制器桩测试；没有将桌面单指鼠标测试宣称为抖音真机双指测试。

## 外部配置与限制

`FEED_RHYTHM_CAT_CONTENT_ID` 已接入用户提供的 `CONTENT15383195906`，获客推荐流精确匹配后进入 `RhythmCatFeedGameScene`。真实配置的路由检查通过；未知 ID、空 ID 与复访继续使用已有兜底。尚未进行抖音后台投放或真机直达验证。

未发布或投放，也未执行真实广告、分享、埋点验证。未取得原游戏的高清素材和源谱面；使用视频的 576×1136 原像素和按观测重建的节奏，因此有录屏压缩及近似时序限制。出处与处理方式见 `marketing/feed/rhythm-cat/reference-notes.md` 和 `asset-provenance.json`。

发布素材约 0.64 MiB，图像解码约 2.92 MiB；参考视频只保存在营销参考目录。

最终固定谱面为 143 个观测单元。最终构建日志报告 2026-09-23 11:08 `build success in 8835`。实际场景截图：`marketing/feed/rhythm-cat/gameplay-preview.jpg`。

最终浏览器实测接住前两份食物显示 2 分，随后漏接三次显示「接住了 2 份冰淇淋」；截图 result-preview.jpg。

## 主页卡片样式补齐（2026-09-23）

按用户截图反馈统一 MathExamCard 与 RhythmCatCard：复用现有橙色笔刷标题边缘、白字深色描边、202×164 封面区域、同款边框和挂环。保留两个卡片的原 Button 引用与周边编辑器布局；两种生成器均调用同一补齐函数，重复执行不覆盖后续编辑。

主页检查覆盖全部 7 张卡片，口算／猫咪场景和入口检查通过。Cocos 日志报告 `build success in 10606`；实际浏览器显示两个完整标题，控制台无警告或错误。效果图：`marketing/feed/rhythm-cat/lobby-cards-unified.jpg`。

## 小幅降低接取难度（2026-09-23）

针对用户反馈的黑猫偏难：修复拖到边界后回拉存在空行程的问题；拖动灵敏度设为 1.1；白猫接取半径由 42 增至 52 源像素，黑猫增加 6 像素补偿，总半径 58；两侧均增加 0.10 秒补接窗口。补接期间食物继续显示并下落，超时每份仍扣一心。143 份食物、原时间点、音轨、素材和三心规则保持原样。

输入和补接回归测试先复现旧行为失败，再确认修复通过。全部 7 组猫咪检查与 TypeScript 检查通过，覆盖两侧边界反向、拖动倍率、窗口内补接／超时扣心、并行待判食物、30/60 FPS 与延迟帧扫掠、最终生命的同时间判定、曲末清算、编辑器参数生效，以及补接食物的显示与移除。

Cocos 日志在 14:50:30 报告 `build success in 9762`，进程退出码仍为 36。实际加载新构建，在 375×740 浏览器验证了黑猫拖动、接住 5 份后的漏接结算、重开恢复 0 分 3 心，控制台无警告／错误。截图：`marketing/feed/rhythm-cat/difficulty-result.jpg`。本轮未进行抖音真机双指测试。

## 看广告减速按钮（2026-09-23）

按用户提供的外卖玩法截图复用同款黄色仪表盘、白字“降速”和红色视频角标，在猫咪下方居中显示。按钮及角标预置到场景，保留已有布局、嘴部与移动边界等编辑器配置；生成器局部追加可重复执行。

接入 `SdkUtils.showRewardedVideo()`，完整观看后本局速度变为 75%，每局一次；食物和音乐同步减速，接续广告前的进度。广告加载与播放期间暂停计时、输入和音频，取消／失败不发奖励；重复点击、销毁或重开后的过期回调不会重复发奖。后台、推荐流退出与广告暂停可叠加。重开恢复正常速度、原音轨和按钮角标。

原曲使用预处理的 75% 速度音轨，保持音高。Cocos 公共 AudioSource 接口在此版本没有统一可用的运行时 playbackRate，因此切换音轨时将音乐时间与谱面时间按比例换算；不依赖私有播放器接口。新增音轨约 0.50 MiB，总发布资源约 1.14 MiB。

7 组猫咪检查、TypeScript 检查、10 项广告归因检查与 18 项既有音频生命周期检查通过。新增覆盖广告完成／取消／异常、单次发奖、暂停期间不掉血、音乐位置换算、静音慢速时钟、后台和推荐流交错、重开、过期回调、图标／角标资源引用，以及序列化 ID 唯一性。

最终 Cocos 日志在 15:19:50 报告 `build success in 8964`，进程退出码 36。375×740 浏览器实际显示指定图标并可点击，领取后显示“慢速”和“已减速25% · 本局生效”，控制台无警告／错误。效果图为 `marketing/feed/rhythm-cat/slowdown-button.jpg` 和 `slowdown-active.jpg`。本地浏览器使用项目的模拟广告成功回调，真实激励广告仍需抖音真机验证。

## 当前速度调整：普通减速15%，广告后减速30%（2026-09-23）

按用户最新要求，两档均以原视频速度为基准：普通模式 0.85 倍，完整观看广告后本局 0.70 倍，不叠乘。重开、广告取消／失败保持或恢复 0.85 倍，普通模式仍可领取减速奖励。按钮提示同步改为“已减速30% · 本局生效”。

普通音轨 `track-normal.mp3` 约 38.431 秒，广告慢速音轨 `track-slow.mp3` 约 46.667 秒；均从原音轨保持音高生成。7 组猫咪检查和 TypeScript 检查通过，包括正常速度、奖励速度、音轨进度换算、重开、广告失败／取消及过期回调。Cocos 日志在 15:39:16 报告 `build success in 7114`，进程退出码 36；实际浏览器确认同款按钮可用、领取后显示减速30%，控制台无警告／错误。`slowdown-active.jpg` 已更新。

## 插屏不主动暂停猫咪（2026-09-23）

按用户反馈，将猫咪场景的广告暂停判断收窄为激励视频。插屏加载／展示不会主动停止谱面计时、音乐或拖动，也不再阻止首触、重开和返回；插屏期间仍禁止叠加请求减速激励广告。实际切后台、推荐流退出、结算及看广告减速的暂停保留。

回归测试先复现插屏阻止首触，再确认修复通过，覆盖插屏期间计时和拖动、关闭不重启音乐、关闭后新触摸、重开、返回及后台暂停交错。猫咪控制器、时间轴、12 项重复插屏检查与 TypeScript 检查通过；本轮未执行抖音真机广告展示验证。
